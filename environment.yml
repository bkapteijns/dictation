name: dictation
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - ffmpeg
  - pip
  - pip:
      - pynput
      - sounddevice
      - soundfile
      - numpy
      - mlx
      - parakeet-mlx
      - pyobjc-framework-Quartz
